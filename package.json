{

  "name": "wideflaresphereclient",
  "version": "1.0.0",
  "repository": "git@wideflare.github.com:wideflare/wideflare.git",
  "author": "Syed Yunus",
  "license": "MIT",
  "type": "module",

  "scripts": {
    "compile" : "rm -r dist/lib && tsc && tsc --build tsconfig.es5.json"
  },

  "devDependencies": {
    "axios": "^1.1.3"
  }
}
 